What does .link() do?
  The .link provides a connection to storage path. 

In your own words (no code), explain how we can use resource interfaces to only expose certain things to the /public/ path.
  We can use resource interface provide access to restricted data.  
